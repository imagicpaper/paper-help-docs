import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'MagicPaper',
  mode: 'site',
  logo: "/newlogo.png",
  favicon: '/newlogo.png'
  // more config: https://d.umijs.org/config
});
